{
  "scripts": {
    "build": "node -e \"const fs=require('fs');fs.mkdirSync('public',{recursive:true});fs.readdirSync('.').filter(f=>f.endsWith('.html')||f==='staticwebapp.config.json').forEach(f=>{fs.copyFileSync(f,'public/'+f)})\""
  }
}
